/**
 * (app)/change-password — Change Password Screen
 *
 * Route file only — delegates to ChangePasswordScreen.
 */

import { ChangePasswordScreen } from '@/src/screens';

export default ChangePasswordScreen;
