/**
 * (app)/enrolled — Enrolled Courses Screen
 *
 * Route file only — delegates to EnrolledCoursesScreen.
 */

import { EnrolledCoursesScreen } from '@/src/screens';

export default EnrolledCoursesScreen;
