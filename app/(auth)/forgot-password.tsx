/**
 * (auth)/forgot-password — Forgot Password Screen
 *
 * Route file only — delegates to ForgotPasswordScreen.
 */

import { ForgotPasswordScreen } from '@/src/screens';

export default ForgotPasswordScreen;
